# Inventory file (contains environment-specific IPs, usernames, etc.)
inventory.yml

# Ansible vault and variable files that may contain secrets
group_vars/

# Ansible retry files
*.retry

# Claude Code local settings
.claude/
